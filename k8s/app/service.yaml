apiVersion: v1
kind: Service
metadata:
  name: cloud-vault-service
  namespace: cloud-vault
spec:
  type: NodePort
  selector:
    app: cloud-vault
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
